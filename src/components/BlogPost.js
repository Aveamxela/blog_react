// import { useParams } from "react-router-dom";

const BlogPost = () => {
  // const {id} = useParams()
  // console.log({id})
  return (
      <>
          {/* <h1>Test blog post{id}</h1> */}
      </>
  );
}

export default BlogPost;
