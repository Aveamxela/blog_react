const BlogPost = ({testsPosts}) => {
  return (
    <>
      <h1>Test</h1>
      {/* {testsPosts.map((post, index) => {
        return (
          <div key={index}>
            <p>{post.id}</p>
            <p>{post.title}</p>
            <p>{post.body}</p>
          </div>
        )
      })}
      {console.log(testsPosts)} */}
    </>
  )
}

export default BlogPost;
